function NotesApplication( ){

	function note(authur, lst){
		this.note = note;
	    this.lst= ["abc", "xyz"]; 
	    return lst;
	}
	var create =function(note_content){
		note_content =this.note;
		

	}
	function listNotes(){
		for (var note_id= 0 ; note_id<=lst.length; note_id++) {
			return this.lst[note_id];
			
		}
		return lst;

	}
}